import {
    InstalledTemplatePackage
} from "./InstalledTemplatePackage";

/**
 * Collection of installed template packages.
 */
export interface InstalledTemplateCollection {

    packages:
        InstalledTemplatePackage[];

}