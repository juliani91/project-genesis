export interface FolderDescriptor {
    /**
     * Relative folder path inside the generated project.
     *
     * Examples:
     * docs
     * Planning/Sprints
     * src/components
     */
    path: string;
}